# Spec-Kit Setup

This directory stores lightweight Spec-Kit configuration and templates for spec-driven development.

Use `specs/` for feature specifications and keep reusable templates in `.specify/templates/`.

