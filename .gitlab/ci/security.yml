security:audit:
  image: python:3.12-slim
  stage: audit
  before_script:
    - python -m pip install --upgrade pip
    - pip install pip-audit trufflehog
  script:
    - pip-audit -r backend/requirements.txt -r ai-service/requirements.txt
    - trufflehog filesystem . --no-update
